# CircularProgressFire

## Описание

`CircularProgressFire` — это класс для создания кругового прогресс-бара с эффектом **огня** на WebGL. 
Позволяет гибко настраивать цвета, скорость и интенсивность пламени.

Эффект рендерится на переданном HTML-элементе `<canvas>`.

[👀 Смотреть Демо](https://webeach.github.io/gl-circular-progress/CircularProgressFire.html)

---

## Сигнатура

```ts
new CircularProgressFire(container: HTMLCanvasElement, options: CircularProgressFireOptions)
```

- **Параметры**
   - `container: HTMLCanvasElement` — элемент canvas, на котором будет отрисован прогресс.
   - `options: CircularProgressFireOptions` — объект конфигурации.

---

## Примеры

### Базовое использование

```ts
import { CircularProgressFire } from '@webeach/gl-circular-progress';

const canvas = document.getElementById('canvas') as HTMLCanvasElement;

const instance = new CircularProgressFire(canvas, {
  colors: [0xff5a00, 0xff9a00, 0xffce00], // Цвета огня
  intensity: 1.0,
  progress: 0.75, // Начальное значение
  speed: 1.5,
  thickness: 20,
});
```

### Динамическое обновление

Все свойства доступны для изменения "на лету" через сеттеры экземпляра класса.

```ts
instance.colors = [0xff0000, 0x00ff00]; // Изменить градиент
instance.intensity = 0.5; // Уменьшить интенсивность огня
instance.speed = 2.0; // Ускорить анимацию
```

---

## API

### Опции (`CircularProgressFireOptions`)

Объект настроек, передаваемый в конструктор.

| Свойство     | Тип        | По умолчанию | Описание                                                                                         |
|--------------|------------|--------------|--------------------------------------------------------------------------------------------------|
| `colors`     | `number[]` | `[0x000000]` | **Обязательно.** Массив цветов в формате HEX (например, `0xff5a00`). Используется для градиента. |
| `intensity`  | `number`   | `1.0`        | Интенсивность эффекта огня.                                                                      |
| `progress`   | `number`   | `0`          | Начальное значение прогресса (от 0.0 до 1.0).                                                    |
| `reversed`   | `boolean`  | `false`      | Направление заполнения. Если `true`, прогресс заполняется против часовой стрелки.                |
| `speed`      | `number`   | `1.0`        | Скорость анимации пламени.                                                                       |
| `startAngle` | `number`   | `0`          | Начальный угол прогресса в градусах.                                                             |
| `thickness`  | `number`   | `10`         | Толщина кольца прогресса в пикселях.                                                             |

### Методы и свойства экземпляра

После создания экземпляра класса доступны следующие методы и свойства:

#### `destroy(): void`

Очищает ресурсы WebGL, удаляет слушатели событий (resize) и останавливает анимацию. Необходимо вызывать перед удалением компонента.

#### Свойства

Следующие свойства можно читать и записывать для динамического обновления:

- `colors`: `number[]`
- `intensity`: `number`
- `progress`: `number`
- `reversed`: `boolean`
- `speed`: `number`
- `startAngle`: `number`
- `thickness`: `number`

---

## Типизация

```ts
import type { CircularProgressFireOptions } from '@webeach/gl-circular-progress';
```

---

## Смотрите также

- [CircularProgressLiquid](./CircularProgressLiquid.md) — эффект жидкости.

