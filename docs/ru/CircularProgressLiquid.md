# CircularProgressLiquid

## Описание

`CircularProgressLiquid` — это класс для создания кругового прогресс-бара с эффектом **жидкости** на WebGL.
Позволяет создавать плавные анимации с настройкой объемного искажения (эффект линзы).

Эффект рендерится на переданном HTML-элементе `<canvas>`.

[👀 Смотреть Демо](https://webeach.github.io/gl-circular-progress/CircularProgressLiquid.html)

---

## Сигнатура

```ts
new CircularProgressLiquid(container: HTMLCanvasElement, options: CircularProgressLiquidOptions)
```

- **Параметры**
   - `container: HTMLCanvasElement` — элемент canvas, на котором будет отрисован прогресс.
   - `options: CircularProgressLiquidOptions` — объект конфигурации.

---

## Примеры

### Базовое использование

```ts
import { CircularProgressLiquid } from '@webeach/gl-circular-progress/CircularProgressLiquid';

const canvas = document.getElementById('canvas') as HTMLCanvasElement;

const instance = new CircularProgressLiquid(canvas, {
  colors: [0x00c6ff, 0x0072ff], // Цвета жидкости
  progress: 0.6, // Начальное значение
  speed: 0.5,
  startAngle: -90, // Начать слева
  thickness: 30,
  volume: 0.5, // Эффект объема/линзы
});
```

### Динамическое обновление

Все свойства доступны для изменения "на лету" через сеттеры экземпляра класса.

```ts
instance.speed = 1.2; // Ускорить течение
instance.volume = 0.8; // Увеличить эффект линзы
instance.reversed = true; // Изменить направление
```

---

## API

### Опции (`CircularProgressLiquidOptions`)

Объект настроек, передаваемый в конструктор.

| Свойство     | Тип        | По умолчанию | Описание                                                                           |
|--------------|------------|--------------|------------------------------------------------------------------------------------|
| `colors`     | `number[]` | `[0x000000]` | **Обязательно.** Массив цветов в формате HEX. Используется для градиента жидкости. |
| `progress`   | `number`   | `0`          | Начальное значение прогресса (от 0.0 до 1.0).                                      |
| `reversed`   | `boolean`  | `false`      | Направление заполнения. Если `true`, прогресс заполняется против часовой стрелки.  |
| `speed`      | `number`   | `1.0`        | Скорость анимации волн жидкости.                                                   |
| `startAngle` | `number`   | `0`          | Начальный угол прогресса в градусах.                                               |
| `thickness`  | `number`   | `10`         | Толщина кольца прогресса в пикселях.                                               |
| `volume`     | `number`   | `0`          | Эффект объема (линзы/3D). Значение от `0.0` (плоский) до `1.0` (сильный эффект).   |

### Методы и свойства экземпляра

После создания экземпляра класса доступны следующие методы и свойства:

#### `destroy(): void`

Очищает ресурсы WebGL, удаляет слушатели событий и останавливает анимацию. Необходимо вызывать перед удалением компонента.

#### Свойства

Следующие свойства можно читать и записывать для динамического обновления:

- `colors`: `number[]`
- `progress`: `number`
- `reversed`: `boolean`
- `speed`: `number`
- `startAngle`: `number`
- `thickness`: `number`
- `volume`: `number`

---

## Типизация

```ts
import type { CircularProgressLiquidOptions } from '@webeach/gl-circular-progress/CircularProgressLiquid';
```

---

## Смотрите также

- [CircularProgressFire](./CircularProgressFire.md) — эффект огня.
