/**
 * Default colors (Black).
 */
export const DEFAULT_COLORS = [0x000000];

/**
 * Default reversed state (false).
 */
export const DEFAULT_REVERSED = false;

/**
 * Default animation speed (1.0).
 */
export const DEFAULT_SPEED = 1.0;

/**
 * Default start angle (0 degrees).
 */
export const DEFAULT_START_ANGLE = 0;

/**
 * Default thickness (10px).
 */
export const DEFAULT_THICKNESS = 10;
